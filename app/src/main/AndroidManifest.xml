<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.vava.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".MainMenuCore">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".SelectGameModCore"/>
        <activity android:name=".AboutPageCore"/>
        <activity android:name=".StatisticsPageCore"/>
        <activity android:name=".HowToPlayPageCore"/>
        <activity android:name=".OptionsPageCore"/>

    </application>

</manifest>